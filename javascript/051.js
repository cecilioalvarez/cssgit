var lista= [2,4,5,8,5];

for (var i=0;i<lista.length;i++) {
    //esto imprime los numeros
    // lo cual no me gusta 
    //quiero imprimir los valores
    if(lista[i]==5) {
        console.log(lista[i]);
    }
   
}

